# 実装仕様書 10 ｜ バーチャルコインで「賭ける」体験（合法版）

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトに、現実の金銭・賞品とは一切関係のない
> **ゲーム内バーチャルコイン**で予想に賭ける機能を追加します。

---

## ⚖️ 0. 法律的な大前提（厳守事項）

**この機能は、日本の刑法186条（賭博罪）に抵触しないよう、
以下を絶対に守る設計とする:**

1. **コインは現実の金銭・賞品・財物と一切交換できない**
2. **コインの売買・購入機能は実装しない**（無料配布のみ）
3. **コインの参加者間の譲渡は不可**（自分のスコアとしてのみ機能）
4. **アプリ内のランキング・バッジ・称号以外には何も影響しない**
5. **「賭け」「ベット」という UI 表現は使えるが、必ず「ゲーム内コイン」と
   明記し、現金との関係を否定するヘッダーを各所に表示**
6. **利用規約・コイン画面・初回案内で「換金不可」を明確に告知**

これを守れば、賭ける**体験**は合法的に提供できる。
コインに財産的価値を持たせないことが運営側のリスクを消す唯一の方法。

---

## 1. 機能の狙い

予想の楽しさは「結果が当たる」だけでなく「**自分の自信に応じて重みをつける**」ことで
何倍にも増える。Superbru や Sportingbet など世界の予想プラットフォームが
バーチャル通貨を入れているのはこの理由。

この機能で:

- 各参加者に**初期1000コイン**を無料配布
- 試合ごとに「**何コイン賭けるか**」を選べる
- 当たれば**オッズに応じた配当**、外れれば没収
- コイン残高で**新ランキング**（コインリッチランキング）
- これが「勝負感」と「自信の表現」を生む

換金は一切できないので、純粋に**ゲーム内のスコア**として機能する。

---

## 2. 大前提（守ること）

- 上記「**法律的な大前提**」を最優先で守る
- **既存機能（spec-01〜09）を一切壊さない**
- 既存の単純予想（spec-01）は**そのまま残す**。コイン賭けは**追加の任意モード**
  （コインを賭けなくても従来の +3pt 採点で予想できる）
- モバイル幅 max 480px で破綻しないこと
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない

---

## 3. データ構造

### 3-1. participant に追加

```js
participant = {
  // ... 既存
  coins: {
    balance: 1000,           // 現在のコイン残高
    totalEarned: 1000,       // 累計獲得コイン（初期付与含む）
    totalLost: 0,            // 累計失ったコイン
    transactions: [          // 履歴（任意・直近20件など）
      { matchId, type: "bet", amount: 100, at: "..." },
      { matchId, type: "win", amount: 250, at: "..." },
      { matchId, type: "lose", amount: 100, at: "..." },
    ],
  },
}
```

### 3-2. matchPredictions に追加

```js
participant.matchPredictions[matchId] = {
  pick: "home",                // 既存
  japanScorer: "kubo",         // spec-09
  betAmount: 100,              // 賭けたコイン（0なら賭けてない）
  payout: null,                // 結果反映後に入る配当（null=未確定、0=没収）
}
```

無い場合のフォールバック: `coins: { balance: 1000, ... }`, `betAmount: 0`

---

## 4. オッズと配当ロジック

### 4-1. オッズの計算（シンプル）

```js
function calculateOdds(participants, matchId, pick) {
  // 同じ大会内の全予想で集計
  const counts = { home: 0, draw: 0, away: 0 };
  participants.forEach(p => {
    const pred = p.matchPredictions?.[matchId];
    if (pred?.pick) counts[pred.pick]++;
  });
  const total = counts.home + counts.draw + counts.away;
  if (total === 0) return 2.0; // 全員未予想なら2倍
  const popularity = counts[pick] / total;
  // 人気が低いほどオッズ高（最低 1.2倍、最高 5.0倍）
  return Math.max(1.2, Math.min(5.0, 1 / (popularity + 0.1)));
}
```

人気のない予想（みんなが選んでない方）に賭けて当てると、配当が大きい。
これが「**逆張りの面白さ**」を生む。

### 4-2. 配当処理

```js
function settleBet(participant, matchId, isHit) {
  const pred = participant.matchPredictions[matchId];
  if (!pred || !pred.betAmount || pred.betAmount <= 0) return participant;
  const odds = pred.odds || 2.0; // 賭けた時点のオッズを保存しておく
  const payout = isHit ? Math.floor(pred.betAmount * odds) : 0;

  return {
    ...participant,
    coins: {
      ...participant.coins,
      balance: participant.coins.balance + payout,
      totalEarned: participant.coins.totalEarned + payout,
      totalLost: isHit ? participant.coins.totalLost : participant.coins.totalLost + pred.betAmount,
    },
    matchPredictions: {
      ...participant.matchPredictions,
      [matchId]: { ...pred, payout },
    },
  };
}
```

採点処理（`applyMatchResults` の中、`scoreMatch` の後）で各参加者に対して呼ぶ。

### 4-3. 賭けた瞬間のコイン控除

予想を入れる時、`betAmount` 分を即座に残高から減らす:

```js
function placeBet(participant, matchId, pick, amount) {
  if (amount > participant.coins.balance) throw new Error("コイン不足");
  const odds = calculateOdds(allParticipants, matchId, pick);
  return {
    ...participant,
    coins: {
      ...participant.coins,
      balance: participant.coins.balance - amount,
    },
    matchPredictions: {
      ...participant.matchPredictions,
      [matchId]: {
        pick,
        betAmount: amount,
        odds,           // 賭けた時点のオッズを固定
        payout: null,
      },
    },
  };
}
```

---

## 5. UI 仕様

### 5-1. 試合カード（PgMatches）の賭けセクション

既存の3択ボタンの下に、**任意のコイン賭けセクション**を追加:

```
[ホーム勝ち] [引き分け] [アウェイ勝ち]
当たれば +3pt

----- 🪙 コインを賭ける（任意） -----
スライダーで賭けるコイン数を選ぶ
[==========●----] 100 / 残高 850
予想が当たれば配当 +250 コイン（オッズ 2.5）
[ 100 コインで賭ける ]
```

- スライダー or 数量入力（10コイン単位、最小10、最大は残高まで）
- リアルタイムで「予想配当」が表示
- 賭けないで普通に予想することも可能（従来通り）
- 一度賭けたら変更不可（キックオフ前まで変更可能でもよいが、簡略化のため変更不可で）

### 5-2. ホーム画面（モードB）に追加

ライブステータスカードに5枚目を追加:

```
┌──────────────┐
│ 🪙 コイン残高   │
│   1,250       │
│   #3位 / 8人中 │
└──────────────┘
```

### 5-3. コインランキングタブ（PgRanking）

既存「⚽試合予想」「🏆大会予想」タブに加えて「**🪙 コインリッチ**」タブを追加:

```
1位  👑 大河    2,450 コイン
2位  🥈 陸     1,800 コイン
3位  🥉 あやこ  1,250 コイン
...
```

### 5-4. コインショップ（無料配布のみ）

「**🪙 コインショップ**」というページを作るが、**現金で買うメニューは作らない**。
代わりに、無料でコインを増やせる仕組みだけを置く:

- **毎日ログインボーナス**: 1日1回 +100 コイン
- **広告視聴**: +200 コイン（広告SDK連携は将来。今は「準備中」表示でよい）
- **予想を10試合入れる**: +500 コイン
- **友達を招待する**: +300 コイン（招待URLからの新規参加で発火）

これで「お金を払わなくてもコインは増やせる」設計になる。

### 5-5. 換金不可の明示

複数の場所で「換金不可」を表示:

```
🪙 ゲーム内コインについて

このコインはゲーム内で予想を盛り上げるための機能です。
・現実のお金や賞品とは一切交換できません
・購入はできません（無料配布のみ）
・他のプレイヤーへの譲渡はできません
```

表示場所:
- `PgCoinShop` のヘッダー
- `PgRanking` の「🪙 コインリッチ」タブの説明
- 初回コイン使用時のモーダル
- 利用規約ページがあればそこにも

---

## 6. 実装ステップ（この順で進める）

- **Step 1**: `participant.coins` のデフォルト値（balance: 1000）と
  `matchPredictions[id].betAmount`/`payout`/`odds` のフォールバック対応
- **Step 2**: `calculateOdds()` を実装
- **Step 3**: `placeBet()` を実装、予想入力画面に組み込む
- **Step 4**: `settleBet()` を実装、`applyMatchResults` の採点処理に組み込む
- **Step 5**: `PgMatches` のUIに「🪙 コインを賭ける」セクションを追加（スライダー付き）
- **Step 6**: ホーム（モードB）のライブステータスに「🪙 コイン残高」カードを追加
- **Step 7**: `PgRanking` に「🪙 コインリッチ」タブを追加
- **Step 8**: `PgCoinShop` ページを新設（毎日ログインボーナス・予想10試合報酬等）
- **Step 9**: **「換金不可」表示**を各所に追加（CoinShop ヘッダー、ランキング、初回利用時）
- **Step 10**: ビルドが通ることを確認

---

## 7. 細かい注意

- **既存予想との互換性**: コインを賭けない予想は従来通り（+3pt のみ）。
  コインを賭けない人を排除しない設計にする
- **負け過ぎ対策**: コインが0になっても「日次ログインボーナス +100」で復活できる。
  借金（マイナス残高）は不可
- **オッズの固定**: 賭けた時点のオッズを保存。後から他の人が予想して
  オッズが動いても、自分のオッズは変わらない
- **賭けの上限**: 1試合あたり「残高の50%まで」など制限を設ける手もあるが、
  今回は制限なし。全額賭けて一発勝負も自由
- **法的表現の徹底**:
  - 「ベット」「賭ける」は使ってOK
  - 「換金」「ペイアウト」「現金化」「賞金」「景品」は使わない
  - 表記は常に「コイン」「ゲーム内コイン」「バーチャルコイン」
  - 「お金を稼ぐ」みたいな表現は避ける

---

## 8. 完了条件（テスト観点）

- [ ] 初回参加時に 1000 コインが付与される
- [ ] 試合予想画面で「コインを賭ける」セクションがあり、スライダーで金額調整できる
- [ ] 予想配当（オッズ × 賭け金）がリアルタイム表示される
- [ ] 賭けた瞬間に残高が減る
- [ ] 試合結果反映時、的中なら配当が残高に加算、外れなら没収
- [ ] ホームのライブステータスにコイン残高が表示される
- [ ] 「🪙 コインリッチ」タブでコイン残高ランキングが見える
- [ ] コインショップ画面で毎日ログインボーナス（+100）が受け取れる
- [ ] 各所に「換金不可」の表示が明確に出ている
- [ ] 既存の単純予想（コイン賭けない）も従来通り動く
- [ ] 既存の spec-01〜09 機能が壊れていない

---

## 9. 実装後

動作確認できたら「優先順位 10 位 完了 / ロードマップ全項目完了」と報告してください。

ここまでで、初期に設定したロードマップ全10項目が完了します。
これでアプリは:
- 試合予想と自動採点
- 採点ルールの可視化
- 30秒で始められる導線
- ヒーロー演出
- 画像シェアでバイラル
- リマインド通知
- 大会内チャット＆リアクション
- 自動結果取得
- バッジ＆ストリーク
- 日本代表特化
- バーチャルコインの賭け体験

を備えた、W杯本番（2026年6月11日）に間に合う完成形になります。
あとはデザインフェーズ（全画面の見た目統一）を残すのみ。
