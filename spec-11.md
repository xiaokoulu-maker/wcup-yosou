# 実装仕様書 11 ｜ 全国ランキング（個人ユーザー横断）

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトに、全ユーザー（全大会の参加者）横断の
> 予想ポイントランキング機能を追加します。

---

## 0. この機能の狙い

今のランキングは「大会内（友達同士）」だけ。これだと:

- 大会を作るのが面倒な**1人ユーザー**が、目標を失いやすい
- 「**全国 ◯位 / 数千人中**」みたいなモチベーションが無い
- 拡散したくなる「全国TOP10入りした」みたいな**勲章**が無い

この機能で:

- **全ユーザー横断のポイントランキング**を導入
- 「1人で予想する」モードが**ただの練習じゃなくて競技**になる
- TOP10入りすると**強烈なシェア動機**になる（spec-04の画像シェアと相性最強）
- 大会を作らないライト層にも「**戻ってくる理由**」が生まれる

---

## 1. 大前提（守ること）

- **既存機能（spec-01〜10）を一切壊さない**
- プライバシー: **ニックネームとアイコンのみ表示**。本名・大会名・所属情報は出さない
- Supabase のクエリは効率重視（大量データを想定）
- モバイル幅 max 480px で破綻しないこと
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない

---

## 2. データ仕様

### 2-1. 集計対象

Supabase の `participants` テーブルから、以下の条件で取得:

```js
// 全 participant エントリのうち、有効なものだけ
SELECT id, nickname, icon, totalMatchPoints, totalPointsBackup, createdAt
FROM participants
WHERE totalMatchPoints > 0   -- 0点は除外（ノイズ削減）
ORDER BY totalMatchPoints DESC
LIMIT 200;                    -- TOP100 + バッファ
```

### 2-2. 集計の単位

**各 participant エントリを「1エントリ」として扱う**。
つまり、同じ人が3つの大会に参加していれば、3エントリ別々にランキング入りする。

理由:
- 今のアプリは匿名（グローバルなユーザーIDが無い）ので、同一人物の判別ができない
- 複数大会に参加するアクティブユーザーが上位に来やすい設計は自然
- 将来「アカウント認証」を入れたら統合できるが、今はこのままが現実的

### 2-3. 表示する情報

- 順位（1位、2位、...）
- ニックネーム（participant.nickname）
- アイコン（participant.icon）
- 累計ポイント（totalMatchPoints）
- 自分自身のエントリは**強調表示**（ハイライト色）

**表示しない情報**:
- 本名
- 所属している大会名
- メールアドレス等の個人情報（そもそも保存されていない）

---

## 3. UI 仕様

### 3-1. ランキング画面への新タブ追加

既存の `PgRanking` に「**🌐 全国ランキング**」タブを追加:

```
[ ⚽ 試合予想 ] [ 🏆 大会予想 ] [ 🪙 コインリッチ ] [ 🌐 全国 ]
```

### 3-2. 全国ランキングの表示構成

```
┌────────────────────────────────┐
│ 🌐 全国ランキング              │
│ ─────────────────────────       │
│ あなたの全国順位                │
│   #312位 / 4,521人中            │
│   累計 24 pt                   │
│ ─────────────────────────       │
│                                │
│ 🏆 TOP 100                     │
│  1位 👑 リュウジ      512 pt   │
│  2位 🥈 サチコ        489 pt   │
│  3位 🥉 タロウ        467 pt   │
│  4位    ヒデキ        450 pt   │
│  ...                            │
│ 100位   ノブオ         28 pt   │
│ ─────────────────────────       │
│                                │
│ 📍 あなたの周辺                 │
│ 307位  ユウキ          25 pt   │
│ 308位  ミカ            25 pt   │
│ 309位  ケンタ          24 pt   │
│ 310位  ヒロシ          24 pt   │
│ 311位  サトシ          24 pt   │
│ 312位  ★ あなた       24 pt   │  ← 強調
│ 313位  アイコ          23 pt   │
│ 314位  リョウ          23 pt   │
│ 315位  ナナ            22 pt   │
│ 316位  タケシ          22 pt   │
│ 317位  ユウタ          21 pt   │
│                                │
│ [ 🔄 更新 ]  [ 📷 順位をシェア ]│
└────────────────────────────────┘
```

### 3-3. 自分が圏外（101位以下）のとき

- TOP100セクションはそのまま
- 「📍 あなたの周辺」セクションで自分を中心に前後5人ずつ

### 3-4. 自分がTOP100にいるとき

- TOP100セクション内で自分を強調
- 「📍 あなたの周辺」セクションは省略

### 3-5. 自分がランクインしてない場合（0pt）

```
あなたは全国ランキングにまだ入っていません。
最初の予想を入れて、全国ランキングに登場しよう！
[ ⚽ 予想する → ]
```

### 3-6. 各エントリの見た目

| 順位範囲 | アイコン | 強調 |
|---------|---------|------|
| 1位 | 👑 | ゴールド色、大きめ |
| 2位 | 🥈 | シルバー色 |
| 3位 | 🥉 | ブロンズ色 |
| 4-10位 | （なし） | 軽い目立たせ |
| 11-100位 | （なし） | 通常 |
| 自分 | ★ | 青強調、左に縦帯 |

---

## 4. シェア機能（spec-04 連携）

「📷 順位をシェア」ボタンで、現在の全国順位をシェアカードとして書き出し:

```
┌─────────────────────────┐
│  W杯 2026 予想メーカー   │
│  全国ランキング          │
│ ─────────────────────    │
│                          │
│    #312位                │
│    / 4,521人中           │
│                          │
│    累計 24 pt           │
│    8試合中 6試合的中     │
│ ─────────────────────    │
│  友達と全国上位を目指そう │
│  xiaokoulu-maker.github.io/wcup-yosou │
└─────────────────────────┘
```

既存の `ShareCardStats` を拡張して、「全国順位 / 全国人数」を表示する。

---

## 5. 実装ステップ（この順で進める）

- **Step 1**: Supabase で全 participant を取得する関数 `fetchGlobalRanking()` を実装
  - `SELECT id, nickname, icon, totalMatchPoints FROM participants WHERE totalMatchPoints > 0 ORDER BY totalMatchPoints DESC LIMIT 200`
  - レスポンスを `[{rank, id, nickname, icon, points}]` の配列に整形
- **Step 2**: 自分の全国順位を計算するロジック
  - 自分の `totalMatchPoints` を、全 participants の中で何位かカウント
  - 「あなたの周辺5人ずつ」も取得
- **Step 3**: `PgRanking` に「🌐 全国」タブを追加
- **Step 4**: 全国ランキングの UI レンダリング（TOP100 + 自分周辺）
- **Step 5**: ランクインしてない人向けの空状態 UI
- **Step 6**: シェアボタンと `ShareCardStats` の全国順位版
- **Step 7**: 「🔄 更新」ボタンで再取得（rate limit: 30秒に1回まで）
- **Step 8**: ホーム画面（モードB）のライブステータスにも「全国順位」を追加（任意・小さく）
- **Step 9**: ビルドが通ることを確認

---

## 6. 細かい注意

- **キャッシュ**: 取得結果を localStorage に5分キャッシュ（過剰アクセス防止）
- **ローディング**: 取得中は「ランキング読み込み中...」のスケルトン表示
- **エラー時**: API失敗時は「現在ランキングを取得できません」と表示し、リトライボタン
- **空状態**: 全 participants が誰もポイント獲得していないとき（W杯開幕前など）は
  「W杯開幕後にランキング開始」と表示
- **アイコン重複**: 同じニックネーム＋同じアイコンの別人がいる可能性。
  画面上は気にしない（順位とポイントで識別される）
- **検索機能**: 今回は実装しない（将来必要なら追加）

---

## 7. パフォーマンス考慮

- 取得は **TOP200まで**に制限（200人で1KB程度）
- 自分の順位計算は別途 `COUNT` クエリ:
  ```js
  SELECT COUNT(*) FROM participants WHERE totalMatchPoints > [自分のポイント]
  // → 自分より上の人数 + 1 が自分の順位
  ```
- 周辺5人ずつは別クエリ（自分の前後でポイントが近い人を取得）
- すべての結果を localStorage で 5分キャッシュ

---

## 8. 完了条件（テスト観点）

- [ ] `PgRanking` に「🌐 全国」タブが追加されている
- [ ] タブを開くと TOP100 が表示される
- [ ] 自分の全国順位（「#◯位 / ◯人中」）が上部に表示される
- [ ] 自分が圏外なら「あなたの周辺5人ずつ」が表示される
- [ ] 自分のエントリは強調表示される
- [ ] 1位は 👑、2位は 🥈、3位は 🥉 のメダルが付く
- [ ] 「🔄 更新」で再取得できる（30秒制限）
- [ ] 「📷 順位をシェア」で全国順位入りのカードが書き出される
- [ ] ランクインしていないユーザー向けの空状態が表示される
- [ ] 既存の spec-01〜10 機能が壊れていない

---

## 9. 実装後

動作確認できたら「優先順位 11 位 完了」と報告してください。
これで個人ユーザーも「全国TOP◯位」というモチベーションを持てるようになり、
SNSでのバイラル拡散も加速します。
