# 実装仕様書 06 ｜ 大会内チャット・リアクション強化

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトのチャット機能を「予想を肴に盛り上がる空間」
> へ進化させます。

---

## 0. この機能の狙い

予想ゲームの本当の楽しみは「予想そのもの」より「**みんなで予想を見せ合って
ツッコミ合うこと**」にある。友達同士で「日本勝つわけないだろｗ」「お前ハーランド
舐めすぎ」みたいな会話が飛び交う空間こそが、ユーザーが**何度も戻ってくる理由**になる。

今のアプリには既にテキストチャットがあるが:
- ただのテキストだけで、予想と切り離されている
- リアクション（いいね、笑、👏）がなく、盛り上がりが見える化されてない
- 「誰の予想に対するコメント」かが分からない

この機能で:

- **予想に対するリアクション**（👍 😂 🤔 🔥）を1タップで送れる
- **「誰の予想にツッコむ」かが明示できる**メンション/引用機能
- **チャットの新着通知**を大会ホームに目立たせる
- 既存のテキストチャットはそのまま残し、上に被せる形で進化

これでチャットが「テキストの羅列」から「**予想を肴にしたタイムライン**」になる。

---

## 1. 大前提（守ること）

- **既存機能を一切壊さない**。spec-01〜05 すべて維持。
- 既存のチャットの DB スキーマ（Supabase の `messages` テーブル等）は
  互換性を保つ。必要なフィールドは「あれば使う、無ければ無視」で実装する。
- リアクション情報の保存は、既存メッセージの JSON フィールド拡張で対応
  （カラム追加するなら **JSONカラム1個追加**まで、複数追加はしない）。
- モバイル幅 max 480px で破綻しないこと。
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない。

---

## 2. 機能仕様

### 2-1. 予想カード（メッセージ内に埋め込み）

ユーザーが試合予想を入れた直後、完了モーダルに「**📣 みんなにこの予想を見せる**」
ボタンを追加。押すと、そのユーザーの予想内容が**チャットに自動投稿**される。

投稿される内容（システムメッセージ風）:

```
🇯🇵 [陸] さんの予想:
日本 vs ドイツ → 日本勝ち！ (+3pt 予定)
6/15 23:00 キックオフ
```

このカードに対して他の参加者が**リアクション**できる（後述）。

### 2-2. ワンタップリアクション

各チャットメッセージ（テキストでも予想カードでも）に、メッセージ下部に
リアクションボタンを4つ常時表示:

```
┌─────────────────────────────┐
│ 「日本勝ち！」                │
│ — 陸 (10:30)                │
├─────────────────────────────┤
│ 👍 3   😂 1   🤔 0   🔥 2    │
└─────────────────────────────┘
```

- タップで自分のリアクション ON/OFF
- 自分が押したものは色付き / 太字でハイライト
- リアルタイム反映（Supabase の購読を流用）

#### データ構造

各メッセージオブジェクトに `reactions` フィールドを追加:

```js
message = {
  id, tournamentId, senderId, senderName, text, type, createdAt,
  reactions: {
    "👍": ["userId1", "userId2", "userId3"],
    "😂": ["userId1"],
    "🤔": [],
    "🔥": ["userId2", "userId4"],
  },
}
```

ない場合は `message.reactions || {}` でフォールバック。

### 2-3. メンション/引用

長押し or 「↩️ 返信」ボタンで、対象メッセージを**引用してコメント**できる:

```
┌─────────────────────────────┐
│ ↩ 「日本勝ち！」(陸)         │
│                              │
│ 「いやドイツ勝つでしょｗ」    │
│ — あやこ (10:35)            │
│ 👍 1   😂 2                 │
└─────────────────────────────┘
```

`message.replyTo = { messageId, senderName, preview }` のフィールドを追加。
表示は薄いグレーの帯で引用元が見える形に。

### 2-4. 未読バッジ（大会ホーム）

モードB（参加済み）のホーム画面の「💬 大会チャット」リンク横に、
**未読件数の赤バッジ**を表示。

- 各ユーザーの `lastSeenMessageAt` を localStorage に保存
  （`wcup_lastSeenChat_<tournamentId>` のキー）
- チャットページに入ったタイミングで更新
- ホームを表示する時に「lastSeenMessageAt より新しいメッセージ件数」をカウント
- 件数 > 0 なら赤バッジで表示。99件以上は「99+」

### 2-5. 採点完了の自動システム投稿

`MatchResultAdmin` で試合結果が確定したとき、チャットに自動でシステム投稿:

```
⚽ 試合結果が出ました！
日本 1-2 ドイツ
🎯 的中した人: 陸, 大河 (+3pt)
😢 外した人: あやこ, 椎菜
```

これで「結果が出た瞬間」の盛り上がりがチャット上で起きる。
（投稿はリアクション可能）

---

## 3. UI 仕様

### 3-1. チャットページのレイアウト

既存の `PgChat` をベースに拡張:

```
┌─────────────────────────────┐
│ ← 戻る   💬 大会チャット      │
├─────────────────────────────┤
│ [システム] ⚽ 試合結果...     │
│ 👍 5  😂 0                  │
├─────────────────────────────┤
│ ↩ 引用：陸の予想             │
│ あやこ: 「いやドイツでしょw」 │
│ 👍 2  😂 3                  │
├─────────────────────────────┤
│ 陸: 「日本勝つって！」        │
│ 👍 1  😂 0  🤔 0  🔥 0      │
├─────────────────────────────┤
│ [入力欄] [送信]              │
└─────────────────────────────┘
```

### 3-2. 予想完了モーダルへの追加

spec-04 で作った完了モーダル:
```
[ 📷 予想を画像で投稿 ]
[ 💬 テキストで招待 ]      ← 既存
[ 閉じる ]
```

ここに**新規ボタン**を1つ追加:
```
[ 📷 予想を画像で投稿 ]
[ 📣 みんなにこの予想を見せる ]   ← 新規（大会内チャットに投稿）
[ 💬 テキストで招待 ]
[ 閉じる ]
```

「📣 みんなにこの予想を見せる」を押すと、現在の大会チャットに予想カード形式で
投稿される（type: "prediction_card"）。

### 3-3. リアクションボタンのデザイン

```css
.reaction-button {
  padding: 4px 10px;
  border-radius: 16px;
  background: #f0f3f8;
  font-size: 13px;
  transition: all 0.15s;
}
.reaction-button.active {
  background: #d6e4ff;
  color: #005BAC;
  font-weight: 600;
}
.reaction-button:active {
  transform: scale(0.92);
}
```

タップ感を出す。長押しでは何もしない（誤タップ防止）。

---

## 4. 実装ステップ（この順で進める）

- **Step 1**: メッセージの型に `reactions` と `replyTo` と `type` を追加
  （既存メッセージは未定義でも動くようフォールバック）
- **Step 2**: `toggleReaction(messageId, emoji)` を実装
  - 既に自分が押してたら配列から削除、なければ追加
  - Supabase に保存、リアルタイム購読で全員に反映
- **Step 3**: 各メッセージ表示に4種リアクションボタンを表示
- **Step 4**: 「📣 みんなにこの予想を見せる」ボタンを完了モーダルに追加し、
  予想カード（type: "prediction_card"）として投稿する処理
- **Step 5**: 試合結果確定時に、システム投稿を自動生成する処理を
  `MatchResultAdmin` に追加
- **Step 6**: 引用/返信機能（`replyTo`）の実装
- **Step 7**: 未読バッジ（localStorage で `lastSeenChat_<tid>` を管理）
- **Step 8**: ビルドが通ることを確認

---

## 5. 細かい注意

- **既存メッセージとの互換性**: `reactions` や `replyTo` が undefined でも
  画面が壊れないよう、必ずフォールバックを入れる
- **リアクション連打防止**: 同じ絵文字を1秒以内に複数回押されても無視
  （UI のレスポンス上、状態は即座に変えてよい。Supabase 書き込みだけ
  デバウンス）
- **メンション**: 今回は「引用返信」だけ実装。`@xxx` のメンション機能は
  別途必要になったら追加
- **絵文字選択**: リアクションは **4種類固定**（👍 😂 🤔 🔥）。
  カスタム絵文字はサポートしない（複雑化を避ける）
- **自動システム投稿の重複防止**: 既に同じ matchId の結果投稿があれば二重投稿しない

---

## 6. 完了条件（テスト観点）

- [ ] 既存のチャットメッセージが今まで通り見える・送信できる
- [ ] 各メッセージの下に 👍 😂 🤔 🔥 のリアクションボタンが表示される
- [ ] リアクションをタップすると、即座に色がついて件数が増える
- [ ] 別端末で見ても、リアクションがリアルタイムで反映される
- [ ] 試合予想完了モーダルに「📣 みんなにこの予想を見せる」ボタンがある
- [ ] そのボタンを押すと、チャットに自分の予想が予想カード形式で投稿される
- [ ] 試合結果が確定したとき、チャットに「⚽ 試合結果」のシステム投稿が出る
- [ ] メッセージを長押し or 「返信」で引用返信できる
- [ ] ホームの「💬 大会チャット」リンクに未読件数の赤バッジが出る
- [ ] チャットを開くと未読が消える
- [ ] 既存の spec-01〜05 機能（予想・採点・シェア・通知等）が壊れていない

---

## 7. 実装後

動作確認できたら「優先順位 6 位 完了」と報告してください。
次は優先順位 7 位（ライブスコア連携で結果自動反映）に進みます。
