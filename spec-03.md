# 実装仕様書 03 ｜ トップ導線の整理（30秒で1予想）

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトのホーム画面を再構成し、
> 初見ユーザーが30秒以内に最初の予想を入れられる導線にします。

---

## 0. この機能の狙い

現状のホーム画面は機能が豊富すぎて、初見ユーザーが「何からやればいいか」迷う。
ワールドカップモード、日本代表モード、AI予想、グループ表、ブラケット、ベスト11、
チャット、大会作成…と入口が多く、コア体験である「予想する」が埋もれている。

この機能で:

- **開いて30秒で「最初の1予想」を入れられる**最短フローを作る
- 二次機能（AI予想・グループ表など）は残すが、目立たせる順序を変える
- ユーザーの状態（未参加 / 参加済み）に応じて、トップに見せる内容を切り替える

これは Twitter, Instagram, TikTok 等が共通でやっている
「**ファーストビューで1つだけアクションを促す**」の鉄則を適用するもの。

---

## 1. 大前提（守ること）

- **既存機能を一切消さない・隠さない**。配置・優先度・見出しの大きさを変えるだけ。
  すべての既存ページ（PgChampion, PgJapan, PgBest11, PgGroups, PgBracket,
  PgChat, PgRanking, PgMatches, PgAdmin 等）はそのまま残し、リンクから到達可能にする。
- Phase A / 採点ルール可視化（spec-01, spec-02）の動作を壊さない。
- 既存のデザイントーン（青 `#005BAC` / 赤 `#E60033` / 日の丸赤）と
  モバイル幅 max 480px を維持。
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない。

---

## 2. 設計の核心

ホーム画面を「**ユーザー状態に応じて切り替わる2モード**」にする。

### モード A：未参加ユーザー（myId が無い・大会に入っていない）

ファーストビューに見せるのは **3つだけ**:

1. **アプリの一行説明**（「W杯を友達と予想して、いちばん当てた人が優勝」のような）
2. **メイン CTA（赤・大）**: 「友達と予想大会を作る」
3. **サブ CTA（青・中）**: 「ひとりで予想する」「参加コードで入る」（横並び2つ）

その下に、折りたたまれた「**もっと遊ぶ**」セクション:
- 🤖 AI 優勝予想を見る
- 📋 グループ表を見る
- 🏆 決勝トーナメント表
- ⭐ ベスト11 を選ぶ
- 💬 みんなのチャット

### モード B：参加済みユーザー（myId がある・大会に紐づいている）

ファーストビューに見せるのは **動的なステータス + 1つの主要 CTA**:

1. **ライブ・ステータス**（横スライドや小さいカード3つで）:
   - 今日の試合数（例：「今日の試合 3」）
   - 次の予想締切までの時間（例：「次の締切 4h12m」）
   - 自分の現在順位（例：「あなたの順位 3位 / 8人中」）
2. **メイン CTA（赤・大）**: 「⚽ 試合を予想する」 → `PgMatches` へ直行
3. **サブ CTA（小）**: 「ランキングを見る」「チャット」（横並び2つ）

その下に、折りたたまれた「**この大会の他の機能**」セクション:
- 🏆 優勝予想・得点王
- 🇯🇵 日本代表予想
- ⭐ ベスト11
- 📋 グループ表
- 💬 大会チャット
- ⚙️ 大会設定（管理者のみ）

### 30秒フロー（モード A の人の場合）

```
[ホーム] → 「友達と大会を作る」をタップ
   ↓
[大会作成画面] → 大会名を入れる（任意・スキップ可）→ 作成
   ↓
[参加画面] → 自分の名前だけ入力 → 参加
   ↓
[ホーム（モード B）] → 「⚽ 試合を予想する」をタップ
   ↓
[試合一覧] → 直近の試合カードをタップ → 3択を選ぶ → 完了
   ↓
[完了モーダル] → 「友達を招待する」or「もう1つ予想する」
```

**この一連を、操作6〜7タップ・所要30秒以内で終えられること**が完了条件。

---

## 3. 具体的な UI 仕様

### 3-1. モード A のホーム画面

```
┌─────────────────────────────┐
│  [ロゴ・アプリ名]           │
│                             │
│  W杯を友達と予想して、       │
│  いちばん当てた人が優勝     │
│                             │
│  ┌─────────────────────┐   │
│  │ 🏆 友達と大会を作る  │   │  ← 赤・大・タップ領域広い
│  └─────────────────────┘   │
│                             │
│  [参加コードで入る][1人で]   │  ← 青・小・横並び
│                             │
│  ── もっと遊ぶ ▼ ──         │  ← 折りたたみ
└─────────────────────────────┘
```

### 3-2. モード B のホーム画面

```
┌─────────────────────────────┐
│  [大会名]                   │
│  [自分のアイコン] [自分の名前]│
│                             │
│  ┌──────┐┌──────┐┌──────┐  │
│  │今日の ││次の  ││順位   │  │  ← 横スクロール可
│  │試合 3 ││4h12m ││3/8位 │  │
│  └──────┘└──────┘└──────┘  │
│                             │
│  ┌─────────────────────┐   │
│  │ ⚽ 試合を予想する     │   │  ← 赤・大
│  └─────────────────────┘   │
│                             │
│  [ランキング] [チャット]    │  ← 青・小・横並び
│                             │
│  ── この大会の他の機能 ▼ ──  │
└─────────────────────────────┘
```

### 3-3. 折りたたみセクションの中身

タップで展開し、既存の各ページへのリンクをグリッド表示する。
2列グリッド、各セルにアイコン + ラベル。

```
┌─────────┬─────────┐
│ 🤖 AI予想│📋グループ│
├─────────┼─────────┤
│ 🏆 決勝T │⭐ベスト11│
├─────────┼─────────┤
│ 💬 チャット│       │
└─────────┴─────────┘
```

---

## 4. 実装ステップ（この順で進める）

- **Step 1**: 既存の `PgHome` の中身をまるごと **`PgHomeLegacy`** にリネーム
  （バックアップとして残し、緊急時に戻せるようにする）
- **Step 2**: 新しい `PgHome` を作り、`myId` の有無でモードA/Bを切り替える
- **Step 3**: モード A の UI を実装（ヒーロー＋3 CTA＋折りたたみ）
- **Step 4**: モード B の UI を実装（ステータスカード＋主CTA＋折りたたみ）
- **Step 5**: ライブ・ステータスのロジック実装:
   - 今日の試合数 = `MATCHES.filter(m => isToday(m.kickoff)).length`
   - 次の締切 = 直近の未予想試合の `kickoff` までの時間差
   - 自分の順位 = `totalMatchPoints` のソート結果から自分の位置
- **Step 6**: 折りたたみセクションの開閉実装（既存リンクを並べるだけ）
- **Step 7**: 30秒フローの動作確認（後述）
- **Step 8**: ビルドが通ることを確認

---

## 5. 細かい仕様

- **折りたたみのデフォルト状態**: 閉じる。タップで開く。
- **「ひとりで予想する」**: 既存の大会作成と同じだが、参加者が自分1人だけ
  （あとから友達を招待することも可能）。実装上は「個人モード」フラグを
  立てるのではなく、「自分だけが参加した大会」として扱う（既存ロジック流用）。
- **大会作成 → 名前入力までの自動繋ぎ**: 大会を作ったあと、参加画面に
  自動的に遷移する（タップを1回減らす）。既に実装済みなら触らない。
- **メイン CTA のボタン**: 既存の `Btn` コンポーネントを使う。色は赤
  （日の丸赤 `#E60033` か、既存の `goldGlow` アニメーション付き）。
- **完了モーダル**: 試合に予想を入れた直後に出すモーダル。
  「もう1試合予想する」「友達を招待する（LINEで共有）」「閉じる」の3択。
  招待は現状テキストシェアでよい（画像シェアは spec-04 で実装）。

---

## 6. 完了条件（テスト観点）

- [ ] 初見ユーザー（myId なし）でホームを開くと、ヒーロー＋3CTA だけが見える
- [ ] 「もっと遊ぶ」を開けば、既存の全ページに到達できる
- [ ] 「友達と大会を作る」→ 名前入れて参加 → ホームに戻るとモード B に切り替わる
- [ ] モード B では、ライブ・ステータス（試合数・締切・順位）が動的に表示される
- [ ] 「⚽ 試合を予想する」のメイン CTA で `PgMatches` に直行できる
- [ ] 試合予想の完了モーダルから「もう1試合予想する」or「友達を招待」が選べる
- [ ] **未参加状態で開いて、1試合目の予想を入れるまでが 30 秒以内・6〜7タップ以内**
- [ ] 既存の優勝予想・日本代表予想・チャット等すべて、ホームの折りたたみ
      経由で従来通り使える（デグレなし）

---

## 7. 実装後

動作確認できたら「優先順位 3 位 完了」と報告してください。
次は優先順位 4 位（予想カードの画像シェア）に進みます。
