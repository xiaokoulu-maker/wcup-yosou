# 実装仕様書 09 ｜ 日本代表特化の深掘り

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトの「日本代表」体験を深く作り込みます。

---

## 0. この機能の狙い

このアプリの最大の強みは「**日本のW杯ファンに刺さる**」こと。
FIFA公式アプリやSuperbruは日本代表中心の世界観を作れない（作る動機がない）。
ここを深掘りすると、競合と圧倒的に差別化できる。

ただし、増やしすぎると散らかる。
**「これがあると日本ファンの心が動く」**4つに絞って作り込む:

1. 次の日本戦カウントダウンを目立たせる
2. 日本選手の得点者予想（誰が決めるか）
3. 試合中の応援チャット（クイック投稿で盛り上がる）
4. 日本戦結果の特別演出（勝利の祝福・敗北の慰め）

---

## 1. 大前提（守ること）

- **既存機能（spec-01〜08）を一切壊さない**。
- 既存の日本代表予想画面（`PgJapan`）は維持。今回はそこを**強化**する形。
- 選手データは**最小限**（フルネーム＋背番号＋ポジション程度）。顔写真は使わない。
- モバイル幅 max 480px で破綻しないこと。
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない。

---

## 2. 機能1：次の日本戦カウントダウン

### 2-1. 表示位置

ホーム画面（モードA・モードB共通）の上部に、**次の日本戦専用カウントダウンバナー**を出す。
既存のヒーロー演出（カウントダウン）の下、または「もっと遊ぶ」の手前。

```
┌──────────────────────────────┐
│ 🇯🇵 次の日本戦                │
│ 🇯🇵 日本 vs 🇩🇪 ドイツ        │
│ あと 2日 14時間 3分           │
│ 6/15 (土) 23:00 キックオフ    │
│ [ ⚽ 日本戦を予想する → ]      │
└──────────────────────────────┘
```

- 背景は日の丸赤系の薄いグラデ（`#E60033` 系を控えめに）
- 試合終了後は次の日本戦に自動切り替え
- 全試合が終わったら「🇯🇵 日本代表お疲れ様でした」のメッセージに変わる

### 2-2. 実装

```jsx
function NextJapanMatchBanner() {
  const next = useMemo(() => {
    const now = Date.now();
    return MATCHES
      .filter(m => (m.home === "日本" || m.away === "日本") && new Date(m.kickoff).getTime() > now)
      .sort((a, b) => new Date(a.kickoff) - new Date(b.kickoff))[0];
  }, []);
  if (!next) {
    return <div className="japan-banner-end">🇯🇵 日本代表お疲れ様でした</div>;
  }
  // カウントダウン処理（spec-03bのHeroCountdownを流用）
  return (...);
}
```

---

## 3. 機能2：日本戦の得点者予想

### 3-1. 仕様

日本戦の試合カード（`PgMatches` 内）で、勝敗予想に加えて
**「日本の得点者を1人選ぶ」**オプションを追加。

```
日本 vs ドイツ
[ホーム勝ち] [引き分け] [アウェイ勝ち]
↑これを選んだら勝敗的中で +3pt

----- 日本が点を取ると思う？（任意） -----
[ 久保建英 ] [ 三笘薫 ] [ 鎌田大地 ] [ 上田綺世 ]
[ 伊東純也 ] [ 浅野拓磨 ] [ 田中碧 ] [ 守田英正 ]
[ 誰も決めない（0-X 負け予想） ]
↑当てれば +5pt ボーナス
```

### 3-2. 選手データ

`JAPAN_SQUAD` として静的に持つ:

```js
const JAPAN_SQUAD = [
  { id: "kubo", name: "久保建英", number: 20, position: "MF" },
  { id: "mitoma", name: "三笘薫", number: 9, position: "MF" },
  { id: "kamada", name: "鎌田大地", number: 15, position: "MF" },
  { id: "ueda", name: "上田綺世", number: 11, position: "FW" },
  { id: "ito", name: "伊東純也", number: 14, position: "MF" },
  { id: "asano", name: "浅野拓磨", number: 18, position: "FW" },
  { id: "tanaka", name: "田中碧", number: 17, position: "MF" },
  { id: "morita", name: "守田英正", number: 6, position: "MF" },
  // ... 必要に応じて追加
];
```

実際のW杯発表メンバーは2026年5〜6月に確定するので、ここは
**「現状の想定メンバー」を入れて、後で簡単に書き換えられる構造**にする。

### 3-3. データ保存

`participant.matchPredictions[matchId]` を拡張:

```js
{
  ...既存,
  japanScorer: "kubo",  // 選手ID、未選択なら null
}
```

### 3-4. 採点

ESPN APIから得点者情報を取るのは難しいので、
**管理者が「日本の得点者」を手入力**できる UI を `MatchResultAdmin` に追加。

```
日本 1-2 ドイツ
日本の得点者: [久保建英 ▼] [追加]
```

得点者リストに対して、自分の予想が一致したら +5pt ボーナス。

#### scoreMatch の拡張

```js
function scoreMatch(prediction, match, extras = {}) {
  let pts = 0;
  // 既存の勝敗的中 (+3pt)
  if (...) pts += SCORING.outcome;

  // 日本得点者ボーナス
  if (prediction.japanScorer && extras.japanScorers?.includes(prediction.japanScorer)) {
    pts += 5;
  }
  return pts;
}
```

---

## 4. 機能3：応援チャット（クイック投稿）

### 4-1. 仕様

日本戦のキックオフ前後の時間帯（**キックオフ1時間前 〜 終了2時間後**）に、
大会内チャット画面の上部に「応援クイック投稿」バーを表示。

```
🇯🇵 応援メッセージを送ろう！
[ 🔥 がんばれ日本！ ] [ ⚽ ゴール！ ] [ 😭 惜しい！ ] [ 🙏 守れ！ ]
[ 🎉 やったー！ ] [ 💔 悔しい！ ] [ 💪 まだ行ける！ ]
```

- タップで即チャットに投稿される（テキスト入力不要）
- 連打防止: 3秒に1回まで
- 試合時間外は非表示

### 4-2. データ

これは既存のチャット投稿機能を使うだけ。
`type: "cheer"` でメッセージタイプを区別。表示は通常のメッセージと同じだが、
小さく「📣 応援」みたいなマークを付ける。

---

## 5. 機能4：日本戦結果の特別演出

### 5-1. 仕様

日本戦の結果が確定した瞬間に、ホーム画面の上部で**特別なフルスクリーン演出**を表示。

#### 勝利時

```
┌──────────────────────────────┐
│        🇯🇵                    │
│      日本勝利！！               │
│   🇯🇵 日本 2-1 ドイツ          │
│                              │
│   おめでとう！                 │
│   ⚽ あなたの予想:             │
│      ✅ 勝敗的中 +3pt          │
│      ✅ 得点者的中 +5pt        │
│   合計 +8pt 獲得              │
│                              │
│   [ 結果をシェア → ]            │
│   [ 閉じる ]                   │
└──────────────────────────────┘
```

- 紙吹雪アニメーション（既存 `confetti` 風があれば、なければシンプルに）
- 日の丸赤と金色のキラキラ演出
- spec-04 のシェアカードボタンに繋げる

#### 敗北時・引き分け時

```
┌──────────────────────────────┐
│      🇯🇵                      │
│   惜しかった…                 │
│   🇯🇵 日本 1-2 ドイツ           │
│                              │
│   次の試合に期待！             │
│   ⚽ あなたの予想:             │
│      予想は外れ 0pt            │
│   [ 閉じる ]                   │
└──────────────────────────────┘
```

- 控えめな演出（青系の落ち着いた色）
- 「次の日本戦まで X日」のリンクで再エンゲージ

### 5-2. 表示制御

- localStorage で「すでに表示した試合ID」を記録（`wcup_japanResultShown_<matchId>`）
- 一度閉じたら再表示しない
- 試合終了から24時間以内のみ表示（古い結果は出さない）

---

## 6. 実装ステップ（この順で進める）

- **Step 1**: `JAPAN_SQUAD` 定数を定義
- **Step 2**: `NextJapanMatchBanner` を実装して、ホームに配置
- **Step 3**: `PgMatches` の日本戦カードに得点者予想セレクタを追加
- **Step 4**: `matchPredictions[id].japanScorer` の保存処理を追加
- **Step 5**: `MatchResultAdmin` に「日本の得点者」入力UIを追加
- **Step 6**: `scoreMatch` を拡張して、得点者的中ボーナス（+5pt）を実装
- **Step 7**: 大会内チャットページに、日本戦時間帯のクイック投稿バーを実装
- **Step 8**: 日本戦結果が確定した直後にフルスクリーン演出を表示する仕組み
- **Step 9**: 表示制御（localStorage で表示済み判定）
- **Step 10**: ビルドが通ることを確認

---

## 7. 細かい注意

- **選手リストは2026年6月の正式メンバー発表前**。仮メンバーで実装し、後で書き換える前提
- **得点者予想は任意項目**。選ばなくても従来の予想は普通に動く
- **時間帯判定**: 応援クイック投稿の「キックオフ1時間前〜終了2時間後」は、
  各日本戦の `kickoff` から計算
- **演出の負荷**: 紙吹雪等は CSS アニメーションで実装。重い JavaScript ループは避ける
- **既存採点との互換性**: `scoreMatch` の `extras` 引数を増やしても、既存呼び出しは
  そのまま動くようデフォルト引数で対応
- **日本戦が無いトーナメント**: 「日本がグループステージ敗退」のシナリオも想定。
  `next` が null の場合の処理を入れる

---

## 8. 完了条件（テスト観点）

- [ ] ホーム上部に「🇯🇵 次の日本戦」バナーが出て、カウントダウンが進む
- [ ] 日本戦の試合カードに「日本の得点者を予想」セレクタが出る
- [ ] 得点者を選んで予想を保存できる
- [ ] 管理者画面で「日本の得点者」を入力できる
- [ ] 結果反映時に、得点者的中ボーナス（+5pt）が加算される
- [ ] 大会内チャットで、日本戦時間帯にクイック投稿バーが出る
- [ ] クイック投稿をタップするとチャットに即時投稿される
- [ ] 日本戦結果確定時にフルスクリーン演出が出る（勝利・敗北で内容違い）
- [ ] 一度閉じた演出は再表示されない
- [ ] 既存の spec-01〜08 機能が壊れていない

---

## 9. 実装後

動作確認できたら「優先順位 9 位 完了」と報告してください。
次は優先順位 10 位（換金性なしバーチャルポイントで「賭ける」体験）に進みます。
これでロードマップ全10項目が完了します。
