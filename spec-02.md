# 実装仕様書 02 ｜ 採点ルールの可視化

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトに、Phase A で作った試合予想・採点機能の
> 「採点ルール」をユーザーに分かりやすく見せる UI を追加します。

---

## 0. この機能の狙い

Phase A で試合別予想・自動採点・ライブ順位を実装したが、
**ユーザーには「どうやって点が入るか」がほぼ見えていない**。

予想ゲームは「採点ルールが直感的に分かる」ことが命。
Superbru（世界260万人のスポーツ予想プール）が支持される理由のひとつは、
「完全的中で◯点／勝敗だけ当てて◯点」みたいな段階ポイントが
全画面でずっと目に入る作りになっていること。

この機能で:

- 「予想したらどう点が入るのか」が**3秒で分かる**
- 結果が出たときに「**+3pt 獲得！**」みたいな手応えが残る
- 「次の試合も予想しよう」と感じる動機が生まれる

---

## 1. 大前提（守ること）

- **既存機能を壊さない。** Phase A で追加した `MATCHES`, `SCORING`, `scoreMatch`,
  `PgMatches`, `MatchResultAdmin`, `PgRanking` の試合予想タブ等はすべて維持。
- `SCORING` 定数（`{ outcome: 3, exact: 2 }`）は触らない。これを参照して
  表示するだけ。ロジック側は変更しない。
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない。
- モバイル幅（max 480px）で破綻しないこと。

---

## 2. 仕様の概要

採点ルールを、以下の **3 箇所** でユーザーに見せる。

### 2-1. 試合一覧ページ（`PgMatches`）のトップに「採点ルールカード」

折りたたみ式のミニカード。タップで詳細展開。

**閉じた状態（デフォルト）の表示例:**

```
┌───────────────────────────────┐
│ ⓘ 採点のしくみ                  ▼ │
│ 勝敗的中で +3pt                   │
└───────────────────────────────┘
```

**開いた状態の表示例（Phase A 時点）:**

```
┌───────────────────────────────┐
│ ⓘ 採点のしくみ                  ▲ │
│                                  │
│ ✅ 勝敗を当てる ……………… +3pt   │
│   （ホーム勝ち / 引き分け /      │
│     アウェイ勝ちを当てる）        │
│                                  │
│ ※ Phase B でスコア予想を追加     │
│   予定（完全的中で +5pt）        │
└───────────────────────────────┘
```

- 既存の青(`#005BAC`)・白基調に合わせる
- カード自体は CSS の既存スタイル（影、角丸）と統一
- 開閉状態は `useState` で持つ。永続化は不要（ページごとにリセットでよい）

### 2-2. 予想入力時の「獲得予定ポイント」ラベル

各試合カードの 3 択ボタン（ホーム勝ち / 引き分け / アウェイ勝ち）の下に、
「**当たれば +3pt**」というラベルを薄く小さく表示。

```
[ホーム勝ち] [引き分け] [アウェイ勝ち]
       当たれば +3pt
```

- ボタンを選択中は「**選択中: +3pt 期待**」のように色を強める
- すでに `SCORING.outcome` から動的に取れるので、数字をハードコードしない

### 2-3. 結果確定時の「獲得点バッジ」

試合の結果が `status: "finished"` になった試合カードで、
自分の獲得点を**バッジ形式で目立たせる**。

| 状態           | 表示                                    |
|----------------|----------------------------------------|
| 的中（+3pt）   | 緑バッジ「**+3pt 的中！**」              |
| 外れ（0pt）    | グレーバッジ「**0pt**」                  |
| 未予想         | 薄いグレー「予想なし」                   |

- 既存の `pulseDot` や `rankOne` などのアニメーションCSSが `styles.css` にあるので、
  的中時だけ軽くキラッと光るアニメーション（`@keyframes` 新規追加可）を入れる
- 外れや未予想は装飾を抑える（祝うのは的中のみ）

---

## 3. 共通コンポーネント設計

採点ルールカードを使い回せるように、コンポーネントを 1 つ作る:

```jsx
function ScoringRulesCard({ defaultOpen = false, compact = false }) {
  // SCORING 定数を参照して動的に表示
  // compact=true なら 1 行だけの簡易版（ランキングページなどに置く用）
}
```

- `defaultOpen`: 最初から開いた状態にするか
- `compact`: 横長 1 行の簡易版（「採点ルール: 勝敗的中 +3pt」だけ）

### 配置箇所まとめ

| 場所                         | バリエーション      |
|------------------------------|--------------------|
| `PgMatches` トップ           | フル版（折りたたみ） |
| `PgRanking` 試合予想タブ上部 | compact 版          |
| `PgAdmin` 「⚽試合結果」タブ | compact 版          |

---

## 4. 実装ステップ（この順で進める）

- **Step 1**: `ScoringRulesCard` コンポーネントを `App.jsx` 内に新設
  （他の `Pg*` コンポーネントと同じ階層に置く）
- **Step 2**: `PgMatches` のトップ（自分の成績サマリーの上か下）に
  フル版を配置
- **Step 3**: 各試合カードの 3 択ボタンの下に「当たれば +Xpt」ラベルを追加
- **Step 4**: 結果確定済みカードに「獲得点バッジ」を追加（的中時のみ軽い演出）
- **Step 5**: `PgRanking` 試合予想タブ、`MatchResultAdmin` 上部に compact 版を配置
- **Step 6**: ビルドが通ることを確認

---

## 5. デザイン指針

- 装飾しすぎない。**「ルールが見える」が最優先**で、派手さは二の次
- 文字サイズは既存 UI と揃える（本文 14〜15px、補助 12px）
- 既存のカラー（`#005BAC` 青、`#E60033` 赤、グレーは `#6B7280` あたり）から外れない
- 的中バッジの「光る」演出は控えめに。やりすぎるとくどい

---

## 6. 完了条件（テスト観点）

- [ ] 試合一覧ページのトップに採点ルールカードが表示され、開閉できる
- [ ] 予想ボタンの下に「当たれば +3pt」が表示される
- [ ] 結果が入った試合で、的中なら「+3pt 的中！」、外れなら「0pt」が見える
- [ ] ランキング・管理者画面にも簡易版の採点ルールが見える
- [ ] `SCORING` 定数の数値を変えると、表示される数字も自動で変わる
      （ハードコードされていない）
- [ ] 既存の Phase A 機能・優勝予想・チャット等が今まで通り動く（デグレなし）

---

## 7. 実装後

動作確認できたら「優先順位 2 位 完了」と報告してください。
次は優先順位 3 位（トップ導線の整理 — 30 秒で 1 予想）に進みます。
