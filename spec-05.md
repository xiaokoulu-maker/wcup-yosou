# 実装仕様書 05 ｜ 試合前リマインド通知（PWA Push）

> このファイルは Claude Code に「読んで実装して」と渡すための仕様書です。
> 既存の `wcup-vite` プロジェクトに、試合キックオフ前にユーザーへ
> 「今日の試合の予想をお忘れなく」とリマインド通知を送る機能を追加します。

---

## 0. この機能の狙い

ここまでで「アプリを開けば30秒で予想できる」状態は作れた。
ただ、ユーザーが**そもそもアプリを開きにこない**と、予想ループが回らない。

W杯期間中（6月11日〜7月19日）は、毎日のように試合がある。
ユーザーがアプリを開かずに試合が終わると、その分の予想機会を逃す＝
**サンクコスト的な体験**になって離脱に繋がる。

この機能で:

- 試合キックオフの**◯時間前にローカル通知**でリマインド
- 「日本戦」など特に注目試合は別枠で目立たせる
- ユーザーが「今日もアプリを開く理由」を能動的に作る

これでリテンションが一段上がる。Duolingo の連続記録通知が成功例。

---

## 1. 技術選定：Web Push か Local Notification か

Webアプリでの通知には2方式ある。

| 方式 | サーバー必要 | iOS Safari 対応 | 実装難度 |
|------|-------------|----------------|---------|
| Web Push（プッシュ通知） | あり（VAPID鍵・サーバー） | iOS 16.4+ で PWAインストール時のみ | 高 |
| **Local Notification（ローカル通知）** | **不要** | **PWAインストール時のみ可** | **低** |

**今回は Local Notification 方式で実装**する。理由：
- Supabase 以外のサーバーを増やしたくない
- ローカルでも「キックオフ前に通知」は実現可能
- PWA としてホーム画面に追加してもらえば iOS でも動く

将来的にどうしても Web Push が必要になったら別途追加。

---

## 2. 大前提（守ること）

- **既存機能を一切壊さない**。spec-01〜04 すべて維持。
- 通知の許可は**ユーザー操作起点で**取得する（いきなりポップアップしない）。
- 通知を**拒否されてもアプリは普通に使える**こと（必須機能にしない）。
- 通知は**ローカルのみ**（アプリを閉じていても OS のスケジューラに乗る仕組み）。
- iOS Safari は PWA（ホーム画面に追加）でないと通知が出ない。これを UI で説明。
- `vite.config.js` の `base: '/wcup-yosou/'` は変更しない。

---

## 3. 機能仕様

### 3-1. 通知の種類（最低3種）

| 種類 | タイミング | 内容例 |
|------|----------|--------|
| 試合前リマインド | キックオフ◯時間前 | 「⚽ 今日の試合が始まります！予想がまだの試合 ◯件」 |
| 日本戦の特別通知 | キックオフ12時間前 | 「🇯🇵 今夜は日本戦！予想して応援しよう」 |
| 結果確定通知 | 試合終了後（結果が入った時） | 「⚽ 試合結果が出ました！あなたは ◯試合中 ◯試合的中（+◯pt）」 |

### 3-2. 通知許可フロー

1. ホーム画面（モードB＝大会参加済み）のどこかに **「🔔 試合通知をオン」ボタン** を配置
2. タップすると `Notification.requestPermission()` でブラウザの許可ダイアログを出す
3. 許可されたら `localStorage.setItem("wcup_notif_enabled", "true")` で永続化
4. 拒否されたら、ユーザーに「ブラウザ設定から後でオンにできます」と説明
5. 一度許可した後は、状態をホーム画面に表示（「✅ 試合通知オン中」）

### 3-3. リマインドのスケジューリング

ローカル通知なので、ブラウザ/PWAが起動している間に
`setTimeout` でスケジュールする。閉じている間は OS の Notification API
（Service Worker 経由）でできる範囲を試す。

#### 実装方針

ユーザーがアプリを開いたとき:

```js
function scheduleNotifications() {
  if (localStorage.getItem("wcup_notif_enabled") !== "true") return;
  if (Notification.permission !== "granted") return;

  const now = Date.now();
  const reminderHoursBefore = 3; // キックオフ3時間前にリマインド

  MATCHES.forEach(match => {
    if (match.status !== "scheduled") return;
    const kickoff = new Date(match.kickoff).getTime();
    const reminderTime = kickoff - reminderHoursBefore * 3600 * 1000;
    if (reminderTime <= now) return; // 過去はスキップ

    const delay = reminderTime - now;
    if (delay > 24 * 3600 * 1000) return; // 24時間以上先はスキップ（次回開いたとき再スケジュール）

    // 日本戦は別メッセージ
    const isJapan = match.home === "日本" || match.away === "日本";
    const title = isJapan
      ? "🇯🇵 もうすぐ日本戦！"
      : "⚽ もうすぐキックオフ";
    const body = isJapan
      ? `${match.away === "日本" ? match.home : match.away} 戦の予想はもう入れた？`
      : `${match.home} vs ${match.away} の予想を入れよう`;

    setTimeout(() => {
      new Notification(title, {
        body,
        icon: "/wcup-yosou/icon-192.png",
        badge: "/wcup-yosou/icon-192.png",
        tag: `match-${match.id}`, // 重複通知を防ぐ
        data: { matchId: match.id, url: "/wcup-yosou/#matches" },
      });
    }, delay);
  });
}
```

アプリ起動時（App コンポーネントの useEffect）でこの関数を呼ぶ。

### 3-4. 結果確定通知

`MatchResultAdmin` で管理者が結果を入力 → `scoreMatch` で全員採点 → 採点後に:

- 自分が予想していた試合の結果が確定したら、その場で `Notification` を発火
- 内容：「⚽ ◯◯ vs ◯◯ の結果が出ました！あなたは **的中** +3pt」（または **外れ**）

これは管理者の操作直後に出るので、リアルタイム性が高い。

### 3-5. PWA インストール案内（iOS 対策）

iOS Safari の場合、Webアプリのままでは通知が出ない。「ホーム画面に追加」して
PWA として開いた場合のみ通知できる。これをユーザーに伝える小さな案内を:

- ホーム画面に「📲 ホーム画面に追加で通知を受け取れます」のヒントを出す
- 検出ロジック: `navigator.userAgent.includes("iPhone") && !window.matchMedia('(display-mode: standalone)').matches`
  （iPhoneかつスタンドアロン起動でない＝Safariブラウザ内）の場合に表示
- 案内テキストは小さく、邪魔にならないように

---

## 4. UI 配置

### 4-1. 通知設定ボタン（ホーム・モードB）

ライブステータスカードの下、または「他の機能」折りたたみの中に:

```
┌───────────────────────────────┐
│ 🔔 試合通知をオン               │
│   キックオフ3時間前にお知らせ    │
└───────────────────────────────┘
```

許可済み時:
```
┌───────────────────────────────┐
│ ✅ 試合通知オン中               │
│   次のリマインド: 6/15 20:00    │
└───────────────────────────────┘
```

### 4-2. iOS用「ホーム画面に追加」ヒント

ホームの一番下に小さく:

```
💡 ヒント: 「共有」→「ホーム画面に追加」で
   通知を受け取れるようになります
```

---

## 5. 実装ステップ（この順で進める）

- **Step 1**: 通知許可関連のユーティリティ関数を追加
  - `requestNotificationPermission()` — 許可リクエスト
  - `isNotificationEnabled()` — 許可状態取得
  - `disableNotifications()` — オフ切り替え
- **Step 2**: `scheduleNotifications()` を実装し、App.jsx の useEffect で呼ぶ
  - アプリ起動時に、今後24時間以内の試合に対して `setTimeout` でスケジュール
- **Step 3**: ホーム画面（モードB）に通知トグルボタンを追加
- **Step 4**: iOS Safari 検出と「ホーム画面に追加」ヒントを追加
- **Step 5**: `MatchResultAdmin` の採点処理後に、自分の予想結果通知を発火
- **Step 6**: ビルドが通ることを確認

---

## 6. 細かい注意

- **通知許可は1度だけ確認**。拒否されたら同じセッションで再度ポップアップしない
- **iconパスは絶対パス**にする（`/wcup-yosou/icon-192.png`）。相対だと PWA で壊れる
- **tag** を使って同じ試合の重複通知を防ぐ
- **タイマー精度**: `setTimeout` は数時間でズレが出ることがある。スケジュール時刻に
  なったときに「過去◯分以内ならまだ通知する、それを超えたら見送る」というガードを入れる
- **タブを閉じると setTimeout は消える**。アプリを開いたら毎回スケジュールし直す設計でOK
- **Service Worker での通知**は将来の拡張用に余地を残しておく。今回は使わない

---

## 7. 完了条件（テスト観点）

- [ ] ホーム画面に「🔔 試合通知をオン」ボタンが見える
- [ ] ボタンを押すとブラウザの通知許可ダイアログが出る
- [ ] 許可後、ボタン表示が「✅ 試合通知オン中」に変わる
- [ ] アプリを開いた状態で、3時間以内に試合がある場合、その時刻にリマインド通知が出る
  （テスト用に reminderHoursBefore を一時的に短くして検証してもよい）
- [ ] 通知の内容に試合名・チーム名が含まれている
- [ ] 日本戦の場合は「🇯🇵 もうすぐ日本戦！」と専用メッセージ
- [ ] 結果確定後、自分の予想が当たった/外れた旨の通知が出る
- [ ] iPhone（Safari）でホーム画面に追加してアプリ起動すると通知が動く
  （許可済みの場合）
- [ ] 拒否しても、通知以外の機能はすべて動く
- [ ] 既存の spec-01〜04 機能（予想・採点・ランキング・シェア等）が壊れていない

---

## 8. 実装後

動作確認できたら「優先順位 5 位 完了」と報告してください。
次は優先順位 6 位（大会内チャット・リアクション強化）に進みます。
